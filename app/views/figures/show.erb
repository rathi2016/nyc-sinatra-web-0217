<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <ul>
      Landmarks:
        <%@figure.landmarks.each do |landmark|%>
        <li><%=landmark.name%></li>
        <%end%>
      </ul>
      <ul>
      Titles:
        <%@figure.titles.each do |title|%>
        <li><%=title.name%></li>
        <%end%>
      </ul>
      <a class="button" href="/">Go Home</a>
  </body>
</html>
